"use client";

import { Footer } from "./Footer";

/**
 * Renders the main site footer on all landing routes (same as homepage).
 */
export function ConditionalLandingFooter() {
  return <Footer />;
}
