# Assets

Place images and graphics here.

## Structure

- **`backgrounds/`** — Full-page background images (e.g. `background.png`)
- **`graphics/`** — Logos, illustrations, decorative elements
- **`icons/`** — Custom icons

## Usage

Reference in code: `/images/backgrounds/background.png` (served from `public/`)
